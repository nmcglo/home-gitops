apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: example-net-wildcard
  namespace: cert-manager
spec:
  secretName: example-net-wildcard-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - "*.example.net"
    - "example.net"
