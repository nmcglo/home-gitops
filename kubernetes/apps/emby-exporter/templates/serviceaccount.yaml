{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "thechart.serviceAccountName" . }}
  labels:
    {{- include "thechart.labels" . | nindent 4 }}
{{- end }} 