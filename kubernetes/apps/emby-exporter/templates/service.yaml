apiVersion: v1
kind: Service
metadata:
  name: {{ include "thechart.fullname" . }}
  labels:
    {{- include "thechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "thechart.selectorLabels" . | nindent 4 }} 