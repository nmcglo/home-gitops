apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-playwright
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-playwright
  ports:
    - port: 3000
      targetPort: 3000
      name: http
