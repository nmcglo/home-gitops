apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tika
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 9998
      targetPort: 9998
  selector:
    app: {{ .Release.Name }}-tika
