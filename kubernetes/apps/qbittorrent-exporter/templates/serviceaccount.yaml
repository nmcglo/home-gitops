{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "qbittorrent-exporter.serviceAccountName" . }}
  labels:
    {{- include "qbittorrent-exporter.labels" . | nindent 4 }}
{{- end }} 